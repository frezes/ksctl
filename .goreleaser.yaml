version: 2

project_name: ksctl

builds:
  - id: ksctl
    main: ./cmd/ksctl
    binary: ksctl
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    flags: [-trimpath]
    ldflags:
      - -s -w -X kubesphere.io/ksctl/pkg/cmd.version=v{{ .Version }}
  - id: unictl-ks
    main: ./cmd/unictl-ks
    binary: unictl-ks
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    flags: [-trimpath]
    ldflags:
      - -s -w -X kubesphere.io/ksctl/pkg/cmd.version=v{{ .Version }}

archives:
  - id: ksctl
    ids: [ksctl]
    formats: [tar.gz]
    name_template: ksctl_{{ .Version }}_{{ .Os }}_{{ .Arch }}
  - id: unictl-ks
    ids: [unictl-ks]
    formats: [tar.gz]
    name_template: unictl-ks_{{ .Version }}_{{ .Os }}_{{ .Arch }}

checksum:
  name_template: checksums.txt

changelog:
  use: git
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
